// SourceGroup - Collapsible group of tracks from a single source.
// Simple text items like Qt's SourceList.

import { VerticalBox, HorizontalBox, Button, GroupBox } from "std-widgets.slint";
import { TrackWidgetData } from "track_widget.slint";

// Simple text item for source list (matches Qt's SourceList)
component SourceTrackItem inherits Rectangle {
    in property <TrackWidgetData> data;
    in property <bool> is-blocked: false;

    callback double-clicked();
    callback context-menu-requested();

    height: 22px;
    background: touch.has-hover ? #3a3a3a : transparent;
    border-radius: 2px;
    opacity: is-blocked ? 0.5 : 1.0;

    touch := TouchArea {
        mouse-cursor: is-blocked ? not-allowed : pointer;

        double-clicked => {
            if (!root.is-blocked) { root.double-clicked(); }
        }

        pointer-event(event) => {
            if (event.button == PointerEventButton.right && event.kind == PointerEventKind.up) {
                root.context-menu-requested();
            }
        }
    }

    HorizontalLayout {
        padding-left: 4px;
        padding-right: 4px;
        spacing: 0px;

        // Simple text like Qt: [V-0] Description...
        Text {
            text: data.summary;
            font-size: 12px;
            color: root.is-blocked ? #666666 : #e0e0e0;
            vertical-alignment: center;
            overflow: elide;
        }
    }
}

export component SourceGroup inherits Rectangle {
    in property <string> title: "Source";
    in property <string> source-key: "";
    in property <[TrackWidgetData]> tracks: [];
    in property <bool> show-configure-button: true;
    in property <[bool]> blocked-tracks: [];
    in-out property <bool> expanded: true;

    callback track-double-clicked(int);
    callback track-context-menu(int);
    callback configure-correlation();

    background: #2a2a2a;
    border-radius: 4px;

    VerticalBox {
        padding: 0px;
        spacing: 0px;

        // Header
        HorizontalBox {
            padding: 4px;
            spacing: 4px;

            // Collapse toggle
            Rectangle {
                width: 16px;
                height: 16px;
                background: collapse-touch.has-hover ? #3a3a3a : transparent;
                border-radius: 2px;

                Text {
                    text: root.expanded ? "▼" : "►";
                    font-size: 9px;
                    color: #888888;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }

                collapse-touch := TouchArea {
                    clicked => { root.expanded = !root.expanded; }
                }
            }

            // Title
            Text {
                text: root.title;
                font-weight: 600;
                font-size: 12px;
                horizontal-stretch: 1;
                vertical-alignment: center;
            }

            // Track count
            Text {
                text: "(" + tracks.length + ")";
                font-size: 10px;
                color: #666666;
                vertical-alignment: center;
            }

            // Configure button
            if root.show-configure-button && root.source-key != "Source 1" && root.source-key != "External": Button {
                text: "Configure...";
                clicked => { root.configure-correlation(); }
            }
        }

        // Content - simple text items
        if root.expanded: VerticalBox {
            padding-left: 8px;
            padding-right: 4px;
            padding-bottom: 4px;
            spacing: 1px;

            for track[idx] in tracks: SourceTrackItem {
                data: track;
                is-blocked: idx < blocked-tracks.length ? blocked-tracks[idx] : false;

                double-clicked => { root.track-double-clicked(track.id); }
                context-menu-requested => { root.track-context-menu(track.id); }
            }

            if tracks.length == 0: Text {
                text: "No tracks";
                font-size: 11px;
                color: #666666;
                font-italic: true;
            }
        }
    }
}
