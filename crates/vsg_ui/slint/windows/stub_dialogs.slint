// Stub dialogs - Placeholder implementations for features not yet implemented.
// These show a "coming soon" message and allow closing.

import { VerticalBox, HorizontalBox, Button } from "std-widgets.slint";
import { Theme } from "../theme.slint";

// StyleEditorDialog - Video preview + subtitle style editing (complex, deferred)
export component StyleEditorDialog inherits Window {
    title: "Style Editor";
    min-width: 800px;
    min-height: 600px;
    preferred-width: 1000px;
    preferred-height: 700px;
    default-font-size: 13px;

    callback close();

    VerticalBox {
        padding: Theme.padding-xl;
        spacing: Theme.spacing-lg;

        // Header
        Text {
            text: "Style Editor";
            font-size: Theme.font-2xl;
            font-weight: 700;
            color: Theme.text-primary;
        }

        Text {
            text: "Video preview and subtitle style editing.";
            font-size: Theme.font-md;
            color: Theme.text-secondary;
        }

        // Placeholder content
        Rectangle {
            vertical-stretch: 1;
            background: Theme.surface;
            border-radius: Theme.radius-lg;

            VerticalBox {
                alignment: center;
                spacing: Theme.spacing-md;

                Text {
                    text: "🎨";
                    font-size: 48px;
                    horizontal-alignment: center;
                }

                Text {
                    text: "Style Editor Coming Soon";
                    font-size: Theme.font-xl;
                    font-weight: 600;
                    color: Theme.text-primary;
                    horizontal-alignment: center;
                }

                Text {
                    text: "This feature will include:";
                    font-size: Theme.font-md;
                    color: Theme.text-secondary;
                    horizontal-alignment: center;
                }

                VerticalBox {
                    alignment: center;
                    spacing: Theme.spacing-xs;

                    Text {
                        text: "• Video preview panel with subtitle overlay";
                        font-size: Theme.font-sm;
                        color: Theme.text-muted;
                        horizontal-alignment: center;
                    }

                    Text {
                        text: "• Style property editing (font, color, size, etc.)";
                        font-size: Theme.font-sm;
                        color: Theme.text-muted;
                        horizontal-alignment: center;
                    }

                    Text {
                        text: "• Font replacement manager";
                        font-size: Theme.font-sm;
                        color: Theme.text-muted;
                        horizontal-alignment: center;
                    }

                    Text {
                        text: "• Live preview of changes";
                        font-size: Theme.font-sm;
                        color: Theme.text-muted;
                        horizontal-alignment: center;
                    }
                }
            }
        }

        // Close button
        HorizontalBox {
            alignment: end;

            Button {
                text: "Close";
                clicked => { root.close(); }
            }
        }
    }
}

// GeneratedTrackDialog - Filter styles from source track (deferred)
export component GeneratedTrackDialog inherits Window {
    title: "Create Generated Track";
    min-width: 500px;
    min-height: 400px;
    default-font-size: 13px;

    callback close();

    VerticalBox {
        padding: Theme.padding-xl;
        spacing: Theme.spacing-lg;

        // Header
        Text {
            text: "Create Generated Track";
            font-size: Theme.font-xl;
            font-weight: 700;
            color: Theme.text-primary;
        }

        Text {
            text: "Filter styles from a source subtitle track to create a new track.";
            font-size: Theme.font-md;
            color: Theme.text-secondary;
            wrap: word-wrap;
        }

        // Placeholder content
        Rectangle {
            vertical-stretch: 1;
            background: Theme.surface;
            border-radius: Theme.radius-lg;

            VerticalBox {
                alignment: center;
                spacing: Theme.spacing-md;

                Text {
                    text: "🔗";
                    font-size: 48px;
                    horizontal-alignment: center;
                }

                Text {
                    text: "Generated Tracks Coming Soon";
                    font-size: Theme.font-lg;
                    font-weight: 600;
                    color: Theme.text-primary;
                    horizontal-alignment: center;
                }

                Text {
                    text: "This feature allows you to:\n• Select styles to include or exclude\n• Create separate tracks for signs/songs\n• Filter dialogue from typesetting";
                    font-size: Theme.font-sm;
                    color: Theme.text-muted;
                    horizontal-alignment: center;
                    wrap: word-wrap;
                }
            }
        }

        // Close button
        HorizontalBox {
            alignment: end;

            Button {
                text: "Close";
                clicked => { root.close(); }
            }
        }
    }
}

// SyncExclusionDialog - Configure frame sync style exclusions (deferred)
export component SyncExclusionDialog inherits Window {
    title: "Frame Sync Exclusions";
    min-width: 450px;
    min-height: 350px;
    default-font-size: 13px;

    callback close();

    VerticalBox {
        padding: Theme.padding-xl;
        spacing: Theme.spacing-lg;

        // Header
        Text {
            text: "Frame Sync Exclusions";
            font-size: Theme.font-xl;
            font-weight: 700;
            color: Theme.text-primary;
        }

        Text {
            text: "Configure which subtitle styles to exclude from frame-level sync adjustments.";
            font-size: Theme.font-md;
            color: Theme.text-secondary;
            wrap: word-wrap;
        }

        // Placeholder content
        Rectangle {
            vertical-stretch: 1;
            background: Theme.surface;
            border-radius: Theme.radius-lg;

            VerticalBox {
                alignment: center;
                spacing: Theme.spacing-md;

                Text {
                    text: "⏭️";
                    font-size: 48px;
                    horizontal-alignment: center;
                }

                Text {
                    text: "Sync Exclusions Coming Soon";
                    font-size: Theme.font-lg;
                    font-weight: 600;
                    color: Theme.text-primary;
                    horizontal-alignment: center;
                }

                Text {
                    text: "This feature allows you to:\n• Exclude certain styles from frame sync\n• Keep signs/typesetting frame-accurate\n• Only adjust dialogue timing";
                    font-size: Theme.font-sm;
                    color: Theme.text-muted;
                    horizontal-alignment: center;
                    wrap: word-wrap;
                }
            }
        }

        // Close button
        HorizontalBox {
            alignment: end;

            Button {
                text: "Close";
                clicked => { root.close(); }
            }
        }
    }
}

// SourceSettingsDialog - Correlation settings per source (tier 2, functional stub)
export component SourceSettingsDialog inherits Window {
    title: "Source Correlation Settings";
    min-width: 500px;
    min-height: 400px;
    default-font-size: 13px;

    in property <string> source-key: "Source 2";

    callback accept();
    callback cancel();

    VerticalBox {
        padding: Theme.padding-xl;
        spacing: Theme.spacing-lg;

        // Header
        Text {
            text: "Correlation Settings for " + source-key;
            font-size: Theme.font-xl;
            font-weight: 700;
            color: Theme.text-primary;
        }

        Text {
            text: "Configure how audio correlation is performed for this source.";
            font-size: Theme.font-md;
            color: Theme.text-secondary;
            wrap: word-wrap;
        }

        // Placeholder content
        Rectangle {
            vertical-stretch: 1;
            background: Theme.surface;
            border-radius: Theme.radius-lg;

            VerticalBox {
                alignment: center;
                spacing: Theme.spacing-md;

                Text {
                    text: "🎯";
                    font-size: 48px;
                    horizontal-alignment: center;
                }

                Text {
                    text: "Source Settings Coming Soon";
                    font-size: Theme.font-lg;
                    font-weight: 600;
                    color: Theme.text-primary;
                    horizontal-alignment: center;
                }

                Text {
                    text: "This feature will allow you to:\n• Select specific audio track for correlation\n• Enable source separation (vocals only)\n• Configure per-source analysis options";
                    font-size: Theme.font-sm;
                    color: Theme.text-muted;
                    horizontal-alignment: center;
                    wrap: word-wrap;
                }
            }
        }

        // Dialog buttons
        HorizontalBox {
            alignment: end;
            spacing: Theme.spacing-sm;

            Button {
                text: "Cancel";
                clicked => { root.cancel(); }
            }

            Button {
                text: "OK";
                clicked => { root.accept(); }
            }
        }
    }
}
