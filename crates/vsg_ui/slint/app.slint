// Main entry point for all Slint UI components
// Re-exports all windows and components for use from Rust

// Theme
export { Theme } from "theme.slint";

// Main windows
export { MainWindow } from "windows/main_window.slint";
export { SettingsWindow } from "windows/settings_window.slint";

// Job workflow dialogs
export { AddJobDialog, SourceInputData } from "windows/add_job_dialog.slint";
export { JobQueueDialog, JobRowData } from "windows/job_queue_dialog.slint";
export { ManualSelectionDialog, SourceGroupData, AttachmentSourceOption } from "windows/manual_selection_dialog.slint";
export { TrackSettingsDialog, LanguageOption } from "windows/track_settings_dialog.slint";

// Stub dialogs (placeholders for future implementation)
export { StyleEditorDialog, GeneratedTrackDialog, SyncExclusionDialog, SourceSettingsDialog } from "windows/stub_dialogs.slint";

// Reusable components
export { TrackWidget, TrackListItem, TrackWidgetData } from "components/track_widget.slint";
export { SourceGroup } from "components/source_group.slint";
export { TrackList, FinalTrackData } from "components/track_list.slint";
export { SourceInput } from "components/source_input.slint";
