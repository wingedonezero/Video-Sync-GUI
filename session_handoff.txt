VIDEO-SYNC-GUI: Session Handoff Notes
======================================
Branch: claude/nice-feistel
Date: 2025-02-15

This document captures all findings, decisions, bugs discovered/fixed, and
current state from the video-verified frame matching sessions.


========================================================================
1. PROJECT STRUCTURE
========================================================================

Main video-verified sync code:
  vsg_core/subtitles/sync_mode_plugins/video_verified/
    matcher.py      - Main orchestrator (calculate_video_verified_offset())
    quality.py      - Frame quality measurement, sequence verification
    candidates.py   - Candidate generation (generate_time_candidates, select_checkpoint_times)
    offset.py       - Sub-frame offset calculation, VFR helpers

Frame utilities:
  vsg_core/subtitles/frame_utils/
    video_reader.py    - VapourSynth/FFMS2 frame extraction, VFR handling
    frame_hashing.py   - SSIM (windowed + global), MSE, perceptual hash comparison

Settings:  vsg_core/models/settings.py
UI:        vsg_qt/options_dialog/tabs.py
Tests:     tests/test_subtitle_data.py (10 tests, all pass)


========================================================================
2. COMMIT HISTORY (oldest → newest on this branch)
========================================================================

c7092500 - Fix video-verified CFR checkpoint distribution and add MSE tiebreaker
  - Added MSE as tiebreaker in candidate scoring (55x more discriminative than SSIM)
  - Fixed checkpoint distribution to spread across video

46a1b443 - Merge of CFR accuracy fixes (commit that "fixed everything" for CFR)
  - 9 checkpoints, search range ±3 frames, sequence length 10
  - This is the baseline that must not regress

d64a05bf - Switch video-verified sync to time-domain search with raw frames
  - Replaced frame-index search with millisecond-based time-domain search
  - generate_time_candidates() steps by source_frame_duration_ms
  - Handles any fps combination (23.976↔29.970, etc.)

65a5960a - Fix frame-aligned offset and improve interlaced sequence discrimination
  - Increased sequence length to 15
  - Fixed calculate_subframe_offset() to snap correctly

9391e33b - Unify video-verified settings and remove content detection
  - Merged ~15 interlaced-specific settings into unified video_verified_* set
  - Removed analyze_content_type() calls (was 5-30s for DVD, up to 30min for idet)
  - Removed deinterlace/IVTC from UI (they produce worse results)
  - Removed content_type parameter from VideoReader calls
  - Single code path for all content types

3521017d - Fix cross-encode frame matching with double-probe SSIM auto-detection
  - Added double-probe: windowed SSIM first, if baseline > threshold → re-probe
    with global SSIM. If global baseline ≤ threshold → use global, no widening.
    If global still > threshold → use global + modest adaptive widening.
  - Changed sort key from (seq_verified, quality, -MSE) to (seq_verified, -MSE)
    removing redundant quality score that masked MSE discrimination
  - Reduced adaptive_margin default from 15 to 8

1a2b735b - Fix cross-fps sequence verification stepping by wrong frame duration
  - verify_frame_sequence() was stepping both source and target by source
    frame duration. For cross-fps pairs (24.405→29.970), this meant target
    jumped ~1.23 frames per step, causing phase-dependent rounding that made
    wrong offsets accidentally score better.
  - Fix: use min(source_frame_duration, target_frame_duration) as step size


========================================================================
3. KEY FINDINGS (DVD/Interlaced/VFR/Telecine)
========================================================================

Content handling:
- Time-domain search (ms offsets) works for ALL content types
- Audio→video offset difference is always integer frames (0-3)
- Deinterlacing (bwdif) gives ZERO improvement for frame matching
- IVTC (VFM+VDecimate) is catastrophically WORSE — never use for matching
- Raw frames (no processing) work fine for finding correct offset
- No content type detection needed — FFMS2 gives raw frames regardless

FFMS2 behavior:
- Progressive: frames as-is
- Soft-telecine: ignores repeat-field flags, gives original ~24fps frames
  - real_fps may be non-standard (e.g., 24.405 instead of 23.976)
  - VideoReader does AssumeFPS to 23.976 for display, but real_fps is used
    for frame index math (matches actual frame count)
- Hard telecine/interlaced: raw frames still match between different encodes
- get_frame_index_for_time() uses _AbsoluteTime timestamps from FFMS2 for
  accurate time→frame mapping (handles VFR correctly)

Audio correlation:
- SCC must use same language (JPN→JPN); eng doesn't match well
- Most MPEG-2 DVDs are effectively CFR (33/34ms alternation, <0.5ms drift)
- VFR detection at 100ms threshold catches true VFR files
- Audio correlation is rock-solid (99%+ match quality) for these test files

SSIM implementations (both in frame_hashing.py):
- Windowed SSIM (scikit-image, use_global_ssim=False):
  - More accurate for same-source/progressive content
  - Produces distances 35-45 for cross-encode DVD content (too high!)
- Global SSIM (mean/variance, use_global_ssim=True):
  - Uses whole-image statistics instead of local windows
  - Tolerant of deinterlace artifacts, resolution differences, encoder differences
  - Produces much lower distances for cross-encode content
  - Auto-selected by double-probe when windowed baseline exceeds threshold


========================================================================
4. CURRENT UNIFIED SETTINGS (settings.py)
========================================================================

video_verified_zero_check_frames: int = 3
video_verified_min_quality_advantage: float = 0.1
video_verified_num_checkpoints: int = 9
video_verified_search_range_frames: int = 8
video_verified_sequence_length: int = 15
video_verified_comparison_method: "ssim"
video_verified_ssim_threshold: int = 10
video_verified_use_pts_precision: bool = False
video_verified_frame_audit: bool = False
video_verified_visual_verify: bool = False
video_verified_adaptive_threshold: bool = True
video_verified_adaptive_margin: int = 8
video_verified_fallback_to_audio: bool = True

Kept for VideoReader (subtitle-anchored sync mode, not video-verified):
  interlaced_deinterlace_method: "bwdif"
  interlaced_use_ivtc: bool = False

IMPORTANT: User's saved config may still have OLD defaults (e.g.,
sequence_length=10 instead of 15). Pydantic loads from saved JSON and
overrides new defaults. User needs to update saved config or reset settings.


========================================================================
5. ALGORITHM FLOW (matcher.py)
========================================================================

1. Get audio correlation delay (pure_correlation_ms)
2. Open VideoReaders with raw frames (deinterlace="none", no IVTC)
3. Get real FPS from FFMS2 (real_fps for index math, fps for display)
4. Generate time-domain candidates: audio_delay ± N * source_frame_duration
5. Select 9 checkpoint times distributed across video
6. Double-probe adaptive threshold:
   a. Probe 3 points with windowed SSIM
   b. If baseline > threshold → re-probe with global SSIM
   c. Decision: use_global_ssim=True, threshold stays or widens modestly
7. For each candidate offset:
   - At each checkpoint: compare source frame at T with target at T+offset
   - Sequence verify: check N consecutive frames match (70% threshold)
   - Sequence step uses min(source_frame_dur, target_frame_dur)
   - Record seq_verified count and MSE
8. Select best: max(seq_verified, then lowest MSE as tiebreaker)
9. Snap to frame-aligned offset via calculate_subframe_offset()
10. Return final_offset = sub_frame_offset + global_shift


========================================================================
6. TEST FILES AND KNOWN-CORRECT DELAYS
========================================================================

Location: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/
Test logs: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/new logs/

Shows: 009-1, Allison & Lillia, Blassreiter, Dragonaut, Hunter x Hunter,
       Starship Operators

Known-correct SCC audio delays (JPN→JPN):
  009-1 S1↔S2:     -356.967ms  (same fps 29.970→29.970)
  009-1 S1↔S3:     -446.050ms  (cross-fps 23.976→29.970)
  Allison S1↔S2:   -119.873ms  (cross-encode 23.976→29.970, diff resolution)
  Starship S1↔S2:    -5.673ms  (soft-telecine 24.405→29.970)
  Starship S1↔S3:    -5.673ms  (soft-telecine 24.342→29.970)

Test case details:
  009-1 S2: 29.970fps → 29.970fps (same fps, both interlaced DVD)
    - Source: 43482 frames, Target: 43470 frames
    - Adaptive: baseline 12.3 → threshold 27 (old code), should be better now
    - CORRECT result: -367.033ms (-11 frames), 9/9 seq_verified

  009-1 S3: 23.976fps → 29.970fps (cross-fps, different encode)
    - Source: 34785 frames, Target: 43470 frames
    - No adaptive widening in old log (threshold stayed at 10)
    - WAS WRONG: picked -404.3ms (-10f) with 5/9, should be -446ms (-11f)
    - Problem: windowed SSIM distances 10-22 caused poor discrimination

  Allison S2: 23.976fps → 29.970fps (cross-encode, diff resolution 704x480 vs 720x480)
    - Source: 35244 frames (FFMS2), Target: 44056 frames
    - Source reported as progressive, Target as interlaced/tff (telecine DVD)
    - Windowed SSIM baseline: 45.0 (huge — different masters/resolutions)
    - Old code widened threshold to 60, killing ALL discrimination
    - WAS WRONG: picked -78.2ms (-2f) with 9/9, should be ~-120ms (-3f)

  Starship S2: 24.405fps → 29.970fps (soft-telecine)
    - Source: 36771 frames (soft-telecine detected), Target: 45180 frames
    - real_fps=24.405 (FFMS2 raw), source_frame_duration=40.975ms
    - No adaptive widening (baseline < 10)
    - WAS WRONG: picked +35.3ms (+1f) with 7/9, should be 0ms
    - Bug: sequence stepping used source frame duration (40.975ms) for both
      sides, causing target (33.367ms/frame) to have inconsistent rounding
    - FIXED in 1a2b735b: use min(source, target) frame duration for stepping

  Starship S3: 24.342fps → 29.970fps (soft-telecine, different encode)
    - Source: 36675 frames, Target: 45180 frames
    - Adaptive: baseline 35.1 → threshold 50 (old code)
    - CORRECT result: 0ms, 8/9 seq_verified


========================================================================
7. BUGS FOUND AND FIXED IN THIS SESSION
========================================================================

Bug 1: Windowed SSIM too strict for cross-encode content
  - Windowed SSIM produces distances 35-45 for different DVD encodes
  - Caused adaptive threshold to over-widen (10→60), killing discrimination
  - FIX (3521017d): Double-probe — if windowed baseline > threshold, re-probe
    with global SSIM. Global SSIM is much more tolerant.

Bug 2: Redundant quality score in candidate sort key
  - Sort was (seq_verified, quality, -MSE) but quality was derived from
    seq_verified, making it a redundant intermediate that masked MSE
  - FIX (3521017d): Changed to (seq_verified, -MSE)

Bug 3: Adaptive margin too large
  - With global SSIM (lower baselines), margin of 15 was excessive
  - FIX (3521017d): Reduced to 8

Bug 4: Cross-fps sequence verification used wrong step size
  - verify_frame_sequence() stepped both source and target by source_frame_duration
  - For cross-fps (24.405→29.970), target jumped ~1.23 frames per step
  - Phase-dependent rounding made wrong offsets accidentally score better
  - FIX (1a2b735b): Use min(source, target) frame duration as step

Bug 5 (NOT A CODE BUG): Sequence length = 10 instead of 15
  - User's saved config overrides new default of 15
  - Need to manually update saved config or reset settings


========================================================================
8. WHAT STILL NEEDS TESTING
========================================================================

- Re-run ALL interlaced test files with the latest 3 commits (3521017d +
  1a2b735b + the sequence step fix) to verify:
  - 009-1 S2 (same fps): still correct
  - 009-1 S3 (cross-fps): fixed by double-probe global SSIM?
  - Allison (cross-encode): fixed by double-probe global SSIM?
  - Starship S2 (soft-telecine): fixed by min-step + MSE tiebreaker?
  - Starship S3 (soft-telecine): still correct?

- Test normal CFR content ("all files 23") to verify no regression from
  the unification changes. CFR same-source content should:
  - Use windowed SSIM (baseline < 10, no global switch)
  - Get 9/9 seq_verified at correct offset
  - Have very low MSE at correct offset

- Update saved config: video_verified_sequence_length from 10 to 15

- Test shows not yet tested: Blassreiter, Dragonaut, Hunter x Hunter


========================================================================
9. ARCHITECTURE NOTES
========================================================================

VideoReader (video_reader.py):
  - Wraps VapourSynth + FFMS2 for frame extraction
  - get_frame_index_for_time(): uses _AbsoluteTime from FFMS2 for VFR accuracy
  - real_fps: pre-AssumeFPS fps (matches actual frame count)
  - fps: post-AssumeFPS fps (used for display/standard calculations)
  - For soft-telecine: AssumeFPS relabels to 23.976, but real_fps stays ~24.4
  - content_type parameter removed from video-verified calls (not needed)
  - deinterlace/IVTC settings still used by subtitle-anchored sync mode

compare_frames (frame_hashing.py):
  - Returns (distance, is_match) tuple
  - distance: (1.0 - SSIM) * 100 for SSIM method (0=identical, 10=SSIM 0.90)
  - use_global_ssim parameter selects between windowed (scikit-image) and
    global (mean/variance) implementations
  - Global SSIM: less sensitive to local pixel differences, better for
    cross-encode/deinterlaced content

generate_time_candidates (candidates.py):
  - Generates offsets in ms: audio_delay + N * source_frame_duration
  - N ranges from -search_range to +search_range (default ±8)
  - Also includes audio_delay itself and 0ms as candidates

select_checkpoint_times (candidates.py):
  - Distributes N checkpoints across video duration
  - Excludes first/last 15% to avoid credits/intros

calculate_subframe_offset (offset.py):
  - Snaps time-domain winner to frame-aligned output
  - integer_frames * source_frame_duration_ms
  - Optionally uses PTS precision from match_details


========================================================================
10. REMOVED FEATURES (intentional)
========================================================================

- Content type detection (analyze_content_type): Removed from matcher.py.
  Was expensive (5-30s for DVD ffprobe, up to 30min for idet full decode).
  Not needed — FFMS2 gives accurate fps, adaptive probe handles cross-encode.

- Deinterlace/IVTC UI widgets: Removed from video-verified settings tab.
  Raw frames produce better matching results than deinterlaced frames.
  Settings still exist in settings.py for subtitle-anchored sync mode.

- Separate interlaced settings: ~15 interlaced_* settings merged into
  unified video_verified_* set. Single code path handles all content.

- generate_frame_candidates(): Dead code deleted from candidates.py.
  Replaced by generate_time_candidates() for time-domain search.


========================================================================
11. CROSS-FPS / INTERLACED FRAME MATCHING TESTING (Session 2)
========================================================================

Context: After reverting the unified interlaced/CFR path (commit 3beccac0)
because IVTC and deinterlacing caused more issues than they solved, we did
exhaustive independent testing to validate that raw frames + time-domain
mapping works across ALL format combinations.

Approach: Standalone Python test scripts using VapourSynth/FFMS2 for frame
extraction, phash + SSIM for match verification, audio correlation for
delay estimation. Tests used 3 checkpoints at 25%/50%/75% of common
duration, 100 frames per checkpoint (300 total), sweep ± N frame offsets
around predicted delay.

------------------------------------------------------------------------
11a. TEST: Starship Operators (same master, CFR same-fps baseline)
------------------------------------------------------------------------
File: test_cross_fps.py
Location: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/

  Ep1: S1 29.970fps CFR ↔ S2 29.970fps CFR (same fps)
    Audio delay: -5.673ms
    Method: Sweep offsets in S1 frame units around audio delay
    Result: 300/300 phash matches at 0 frame offset (0ms)
    Confirms: baseline same-fps same-master matching works perfectly

  Ep2: S1 29.970fps CFR ↔ S2 23.976fps CFR (cross-fps)
    Audio delay: -991.610ms
    Method: Same sweep, offsets in S1 frame units
    Result: 300/300 phash matches at -30 S1-frames (-1001ms)
    Key finding: PTS delta is stable across all checkpoints while index
    offset grows over time. PTS delta is the meaningful value for mkvmerge,
    not frame index offset.

------------------------------------------------------------------------
11b. TEST: Allison & Lillia (interlaced ↔ progressive, cross-fps)
------------------------------------------------------------------------
File: test_cross_fps.py

  S1 29.970fps CFR interlaced TFF (720x480) ↔ S2 23.976fps CFR progressive (704x480)
    Audio delay: -119.873ms
    Method: Same sweep in S1 frame units
    Result: 300/300 phash matches at -2 S1-frames (-66.7ms)
    Key finding: phash handles interlaced vs progressive with no
    deinterlacing needed. The 32x32 phash downscale naturally low-pass
    filters away interlace comb artifacts. Different resolutions
    (720x480 vs 704x480) also handled fine by phash downsampling.

------------------------------------------------------------------------
11c. TEST: Blassreiter (3 sources, cross-format, cross-master)
------------------------------------------------------------------------
File: test_blassreiter.py
Location: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/Blassreiter/

Sources:
  S1: 720x480 29.778fps VFR soft-telecine (progressive, 50/33ms gaps) 42195f
  S2: 720x480 29.970fps CFR interlaced TFF 42468f
  S3: 1280x720 23.976fps CFR progressive (bluray/webdl) 34529f

Audio correlation (JPN tracks must match language AND format: 5.1↔5.1):
  S1↔S2 (5.1↔5.1 JPN): +1026ms, conf 0.49 (low but consistent)
  S1↔S3 (stereo JPN): +144ms, conf 0.98
  S2↔S3: -880ms, conf 0.22

Results:
  S1↔S2 (same resolution, VFR↔CFR, progressive↔interlaced):
    300/300 matches at +30 frames = +1001ms, avg phash 1.48
    Works: phash handles VFR↔CFR AND progressive↔interlaced simultaneously

  S1↔S3 (different master, 720x480 DVD ↔ 1280x720 BD):
    0/300 matches at ALL offsets, avg phash ~30
    Fails: completely different masters, luma mean 106 vs 151

  S2↔S3 (different master, 720x480 DVD ↔ 1280x720 BD):
    0/300 matches at ALL offsets, avg phash ~31
    Fails: same reason as S1↔S3

  Additional metric sweep (phash, dhash, ahash, whash, ssim, mse, ncc)
  every 5s across full duration for S1↔S3: No metric consistently works
  for true different-master (DVD↔BD). Two lucky hits at t=35s and t=90s
  across all metrics, everything else is noise.

Key finding: phash works for same-master cross-format (interlaced↔
progressive, VFR↔CFR, different resolutions within same master).
Completely fails for genuinely different masters (DVD↔BD) where the
source material was authored differently with different color grading,
resolution, and encoding. Audio correlation is the only option for
true different-master content.

------------------------------------------------------------------------
11d. TEST: VideoTimestamps library for time-domain frame mapping
------------------------------------------------------------------------
File: test_videotimestamps.py
Location: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/
Library: https://github.com/moi15moi/VideoTimestamps (v1.1.0)

Purpose: Instead of sweeping frame index offsets and converting with
average frame durations (lossy for VFR), use the VideoTimestamps library
to do exact PTS-based frame↔time mapping.

API pattern:
  from video_timestamps import VideoTimestamps, TimeType
  from fractions import Fraction
  from pathlib import Path

  vts = VideoTimestamps.from_video_file(Path(video_path))
  # frame_to_time returns SECONDS as Fraction (not ms!)
  ref_time = vts.frame_to_time(frame_num, TimeType.EXACT)
  # time_to_frame takes seconds as Fraction, returns int frame
  frame = vts.time_to_frame(time_fraction, TimeType.START)
  # pts_list contains ms values, time_scale = 1000

  # For files where video isn't stream 0 (e.g. subtitle first):
  vts = VideoTimestamps.from_video_file(path, index=1)

Mapping pattern (ref frame → target frame with delay):
  delay_s = Fraction(int(delay_ms * 1000), 1000000)
  ref_time = vts_ref.frame_to_time(ref_frame, TimeType.EXACT)
  tgt_time = ref_time + delay_s  # positive = tgt ahead of ref
  tgt_frame = vts_tgt.time_to_frame(tgt_time, TimeType.START)

Sign convention: tgt_time = ref_time + delay
  Positive delay = target content is temporally ahead of reference
  (e.g., if S2 plays 1 second of black before S1 starts, delay = +1000ms)

Tested on confirmed-matching pairs:

  Starship Ep1 (29.970↔29.970 CFR, 0ms delay):
    300/300 matches — trivial case, validates the mapping is correct

  Starship Ep2 (29.970↔23.976 CFR, +1001ms delay):
    299/300 matches — cross-fps with exact Fraction arithmetic
    Initially got 37/300 due to two bugs:
      Bug 1: frame_to_time returns seconds, code divided by time_scale
              unnecessarily in some paths (unit confusion)
      Bug 2: delay sign was wrong (-1001 instead of +1001). In test_cross_fps
              convention, offset=-1001 meant S1 behind S2. In VideoTimestamps
              test with ref=S1, tgt=S2, since S2 is AHEAD, delay must be
              +1001 (tgt_time = ref_time + positive_delay)

  Blassreiter S1↔S2 (29.778 VFR ↔ 29.970 CFR, +1001ms delay):
    300/300 matches — VFR↔CFR with exact PTS mapping

Key finding: VideoTimestamps provides clean exact frame↔time mapping
using Fraction arithmetic. Properly handles VFR, different CFR rates,
and any FPS combination. The time-domain approach (map through real PTS)
is fundamentally correct for all cases unlike index-offset approaches
that drift with different frame rates.

------------------------------------------------------------------------
11e. OVERALL FINDINGS FROM TESTING
------------------------------------------------------------------------

What works for frame matching (no deinterlace/IVTC needed):
  - Same-fps CFR ↔ CFR: phash perfect (ph=0)
  - Cross-fps CFR ↔ CFR: phash perfect with correct time-domain offset
  - Interlaced ↔ progressive (same master): phash works, comb filtered
    by 32x32 downscale
  - VFR ↔ CFR (same master): phash works with PTS-based mapping
  - Different resolution same master (720x480 ↔ 704x480): phash works
  - Different encode same master (different BD releases): app's SSIM
    verifier works with 9 checkpoints + voting (phash_exact=85/90)

What does NOT work:
  - True different master (DVD ↔ BD): no visual metric works. Different
    color grading, resolution, luma levels. Only audio correlation.

Audio correlation notes:
  - Must match language AND channel format (5.1↔5.1, stereo↔stereo)
  - Wrong track selection gives ~0.21 confidence vs 0.98 correct
  - First chunk can be noisy (silence/intro), "first stable" mode handles it
  - Correlation gets the delay within ~1 frame, video verifier refines

VideoTimestamps vs index offset:
  - Index offset grows over time with different FPS
  - PTS delta stays constant — this is what mkvmerge needs
  - VideoTimestamps provides exact PTS↔frame mapping via Fraction math
  - Avoids floating-point drift from average frame duration calculations


========================================================================
12. NEURAL FEATURE SEQUENCE SLIDING (Session 3)
========================================================================

Context: Explored whether AI/neural visual features could verify frame
offsets more robustly than per-frame SSIM/phash, especially for
cross-encode content. Tested many approaches; most failed at per-frame
matching but sequence sliding was a breakthrough.

------------------------------------------------------------------------
12a. PER-FRAME NEURAL MATCHING: ALL FAILED
------------------------------------------------------------------------

Test files: Outbreak Company EP3, Source 1 ↔ Source 2
  Same master, same fps (23.976↔23.976), ground truth offset: -23 frames
  Test: 90 checkpoints across episode, sweep ±30 offsets per checkpoint,
  check if the model picks offset=0 (correct match) as most similar.

Results (per-frame, higher = better):
  DINOv2 ViT-B/14 (224×224 CLS):          34/90 (37.8%) — best per-frame
  DINOv3 ViT-S/16 (448×448):               2/90 (2.2%)
  DINOv3 ViT-B/16 (448×448):               1/90 (1.1%)
  DINOv3-B + Danbooru ResNet50 (2816-dim):  2/90 (2.2%)
  ISC ft_v107 (256-dim):                    0/90 (0.0%)
  DnS descriptor:                           0/90 (0.0%)
  S2VS (ViSiL head):                        0/90 (0.0%)
  LPIPS VGG:                                1/90 (0.0%)
  DreamSim:                                 0/90 (0.0%)

Why per-frame fails: Within static anime scenes, neighboring frames are
virtually identical to ANY vision model. A frame at offset +5 looks just
as similar as offset 0. The models aren't broken — single frames don't
carry enough temporal signal for same-master content.

Test files created in: /home/chaoz/Desktop/Makemkv/Test Files/Outbreak Company/
  test_ep3_dinov3_perframe.py, test_ep3_dinov3_danbooru.py,
  test_ep3_lpips_dreamsim.py, test_ep3_isc_perframe.py

------------------------------------------------------------------------
12b. SEQUENCE SLIDING: BREAKTHROUGH (100% accuracy)
------------------------------------------------------------------------

Approach (user's idea): Instead of matching individual frames, slide a
10-second SEQUENCE from Source 1 across a wider window in Source 2.
At each slide position, compute mean cosine similarity across all
aligned frame pairs. The position with the highest cumulative score
is the correct offset. Like visual cross-correlation.

Test: Outbreak Company EP3, Source 1 ↔ Source 2
  Same master, same fps (23.976↔23.976)
  10-second window, ±5s slide range, 9 positions (10%–90% of episode)

  ISC (256-dim):
    9/9 exact match (100%), 239/239 frame matches at every position
    Mean score: 0.9993, range [0.9987, 0.9996]
    ~23s per position

  DINOv3 ViT-B/16 (768-dim):
    9/9 exact match (100%), 239/239 frame matches at every position
    Mean score: 0.9989, range [0.9984, 0.9993]
    ~55s per position

Score landscape (DINOv3-B, 50% position) — perfect symmetric gradient:
  off= +15: 0.9880    off= +20: 0.9948    off= +23: 0.9992 ★ PEAK
  off= +16: 0.9894    off= +21: 0.9962    off= +24: 0.9977
  off= +17: 0.9909    off= +22: 0.9977    off= +25: 0.9962
  off= +18: 0.9923                         off= +26: 0.9948
  off= +19: 0.9936                         off= +31: 0.9880
Drops ~0.0014 per frame from peak, perfectly symmetric both sides.
Completely unambiguous — no false peaks anywhere in ±5s range.

How the sliding works:
  1. Extract ISC/DINOv3 features for every frame in a 10s source window
  2. Extract features for a wider target window (10s + 2×5s padding)
  3. Slide source sequence across target one frame at a time
  4. At each slide position p: pair src[i] with tgt[p+i], compute
     cosine similarity for each pair, take mean across all pairs
  5. Position with highest mean = correct offset
  Key function: slide_and_score() — normalize features, dot product

ISC is the better choice: 2.3× faster, slightly higher scores, 256-dim
vs 768-dim features. ISC = Image Similarity Challenge (Meta ISC21),
designed for near-duplicate image detection — exactly this use case.

Test file: test_ep3_sequence_slide_v2.py

Sign convention note: The sliding finds that source frame N's content
appears at target frame N+23 (offset = tgt_frame - src_frame = +23).
The audio correlation reports -959ms / -23 frames using the opposite
convention (delay to apply). Same information, different sign.

------------------------------------------------------------------------
12c. INTERLACED TEST: SEQUENCE SLIDING FAILS (different fps)
------------------------------------------------------------------------

Test: Starship Operators EP2, Source 1 ↔ Source 2
  Source 1: 29.970fps interlaced (MPEG2, field_order=tt)
  Source 2: 24.405fps progressive with 2:3 pulldown
  Audio correlation: -991.610ms

Results (frame-by-frame sliding, no preprocessing):
  ISC: 0/8 positions, scores 0.13–0.28, 0/299 matches, random offsets
  DINOv3-B: 0/8 positions, scores 0.27–0.66, 0/299 matches, random

Total failure because:
  1. Different frame rates (29.970 vs 24.405) — frame-by-frame sliding
     doesn't work, frames don't correspond 1:1 in time
  2. Interlaced vs progressive — pixel content looks fundamentally different
     (combing artifacts vs clean frames)
  3. Both are originally 24fps content telecined differently

Attempted fix: bwdif deinterlace on Source 1 + time-based sampling
(sample every 100ms, map to nearest frame in each source). Test was
stopped before completion — needs further investigation into proper
IVTC for both sources to recover original 24fps content before
comparing.

Test files: test_ep2_sequence_slide.py, test_ep2_slide_deinterlaced.py

------------------------------------------------------------------------
12c-2. DEINTERLACED TEST: 009-1 EP1 (same fps works, cross fps fails)
------------------------------------------------------------------------

Test: 009-1 EP1, ISC with bwdif deinterlacing

Sources:
  S1: 29.970fps interlaced TFF, 720×480 (43470 frames) — deinterlaced
  S2: 29.970fps interlaced TFF, 720×480 (43482 frames) — deinterlaced
  S3: 23.976fps progressive, 704×384 (34785 frames) — already progressive

Audio correlation: S1↔S2 = -356.967ms, S1↔S3 = -446.050ms

S1↔S2 (same fps, both deinterlaced with bwdif): WORKS
  8/9 consensus at +22 frames (+734.1ms), 1 outlier at +24f
  299/299 matches at every position
  Mean score: 0.9956, range [0.9932, 0.9974]
  Deinterlacing was sufficient — same resolution, same fps, same master

S1↔S3 (cross fps 29.970↔23.976, deinterlaced↔progressive): FAILED
  0/100 matches at every position, random offsets
  Scores near zero: mean 0.134, range [-0.003, 0.350]
  Score landscape completely flat — no peak at all
  Different resolution (720×480 vs 704×384) + different fps makes
  ISC unable to recognize the content as matching

Deinterlace method: VapourSynth bwdif (bob-weave deinterlace filter)
  core.std.SetFieldBased(clip, 2)  # mark TFF
  core.bwdif.Bwdif(clip_marked, field=2)  # auto detect, same framerate
  Does NOT change frame rate or frame count — just cleans combing in place.

Key finding: bwdif deinterlacing solves same-fps interlaced↔interlaced
matching. Cross-fps + different resolution remains unsolvable by neural
sequence sliding — must fall back to audio correlation only.

Test file: /home/chaoz/Desktop/Makemkv/Test Files/Interlaced/009-1/
           test_ep1_sequence_slide.py

------------------------------------------------------------------------
12c-3. APP METHOD vs ISC: Head-to-head on 009-1 S1↔S2 (deinterlaced)
------------------------------------------------------------------------

Same test pair: 009-1 EP1 S1↔S2, both bwdif deinterlaced, same fps.

App's current method (phash/SSIM/MSE, 9 checkpoints, ±8f search):
  WRONG ANSWER: picked +0ms (should be ~-357ms / -11 frames)
  Every candidate: 1/9 seq_verified — ZERO discrimination
  SSIM distances: 31–35 across ALL candidates (threshold=10)
  phash distances: 14–18 across ALL candidates (threshold=5)
  Only 15–22/135 frames matched (11–16%)
  Even the correct offset (-11f) has avg_ssim=32.8 — virtually
  indistinguishable from wrong offsets (avg_ssim=31.1 at 0f)

ISC sequence sliding (same pair, same deinterlacing):
  CORRECT ANSWER: 8/9 exact at +22 frames
  Scores: 0.9932–0.9974, 299/299 matches at every position
  Clear unambiguous peak with smooth gradient

Conclusion: The app's phash/SSIM/MSE approach cannot discriminate
between candidate offsets on interlaced DVD content, even with
deinterlacing. The pixel-level differences between different DVD
encodes are too large (SSIM ~0.67). ISC's learned features see
through encoding differences that pixel metrics cannot.

Test file: test_ep1_app_method.py

------------------------------------------------------------------------
12d. MODELS AND DEPENDENCIES INSTALLED
------------------------------------------------------------------------

ISC feature extractor:
  pip install isc-feature-extractor
  Usage: from isc_feature_extractor import create_model
  model, preprocess = create_model(weight_name='isc_ft_v107', device='cuda')
  Features: 256-dim per image, ResNet-based, fast

DINOv3 ViT-B/16:
  Installed via: pip install dinov3easy (wrapper package)
  Weights downloaded to /tmp/dinov3_weights/:
    dinov3_vitb16_pretrain_lvd1689m.pth (327MB)
    dinov3_vitl16_pretrain_lvd1689m.pth (1.2GB)
  Requires HuggingFace gated repo access for official weights
  Local repo path for torch.hub.load:
    .venv/.../dinov3easy/dinov3/
  Had to fix hardcoded checkpoint path in:
    .venv/.../dinov3easy/checkpoints/checkpoints_location.txt

RF5/danbooru-pretrained (PyTorch anime ResNet50):
  Loaded via torch.hub: torch.hub.load('RF5/danbooru-pretrained', 'resnet50')
  Model structure: Sequential(backbone_conv_layers, head)
  Backbone features: model[0] → (B, 2048, H, W) → avgpool → (B, 2048)

TransVCL (video copy detection):
  Cloned to /tmp/TransVCL/, model at transvcl/weights/model_1.pth
  Uses ISC features at 1fps → Transformer → segment detection
  Found segments correctly but per-frame matching still failed

------------------------------------------------------------------------
12e. KEY TAKEAWAYS
------------------------------------------------------------------------

1. Sequence sliding works perfectly for same-master same-fps content.
   ISC is the recommended model (faster, higher scores, simpler).

2. Per-frame neural matching is fundamentally wrong for this use case.
   All 9+ models tested failed. The problem is the content (static
   anime scenes where neighboring frames are nearly identical), not
   the models.

3. bwdif deinterlacing + same-fps sliding WORKS. 009-1 S1↔S2: both
   interlaced 29.970fps deinterlaced with bwdif → 8/9 exact, 0.995+
   scores, 299/299 matches. Deinterlacing does NOT change frame rate
   or frame count — just cleans combing artifacts in place.

4. Cross-fps + different resolution FAILS even with deinterlacing.
   009-1 S1↔S3 (29.970↔23.976, 720×480↔704×384): scores near zero.
   Starship Operators S1↔S2 (29.970↔24.405): same failure.
   Must fall back to audio-correlation-only for these pairs.

5. For production, video-verified mode needs to detect pair type:
   - Same fps (or close): use ISC sequence sliding for verification
   - Cross fps / interlaced↔progressive with different resolution:
     skip visual verification, trust audio correlation only
   - Gate: if either source is MPEG2 interlaced/telecine AND fps
     differs significantly, use audio-only path. But pure progressive
     MPEG2 at same fps should still use visual verification.

6. For production: sequence sliding centered on the audio correlation
   offset would give frame-exact verification with ±a few frame search.
