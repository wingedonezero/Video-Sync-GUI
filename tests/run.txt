pytest -q from main directory


PYTHONPATH=. pytest -q


Before:

# vsg_core/pipeline.py  (finally:)
if not and_merge or merge_ok:
    shutil.rmtree(job_temp, ignore_errors=True)


    After (keep temp on success):

    # vsg_core/pipeline.py  (finally:)
if not and_merge or not merge_ok:
    shutil.rmtree(job_temp, ignore_errors=True)
