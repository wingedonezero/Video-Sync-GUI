# Video Sync GUI - Python Dependencies
# Install with: pip install -r requirements.txt

# Core GUI Framework
PySide6

# Scientific Computing & Analysis
numpy
scipy
scikit-learn

# Audio Analysis - REQUIRED for correlation, onset detection, DTW, spectrogram analysis
librosa
numba>=0.61.0
llvmlite>=0.43.0

# Subtitle Processing
pysubs2

# Font Parsing (for font replacement feature)
fonttools

# Spell Checking
pyenchant

# Unicode Regex Support (for SubtitleEdit OCR patterns with \p{L}, \p{Ll}, etc.)
regex

# OCR (Tesseract wrapper - default)
pytesseract

# Optional OCR backends - install for better accuracy:
# EasyOCR (deep learning, better for varied fonts):
#   pip install easyocr
# PaddleOCR (state-of-art accuracy):
#   pip install paddleocr

# XML Processing
lxml

# Video/Audio Processing
av

# Image Processing
Pillow

# Frame/Video Timing
VideoTimestamps

# Frame Matching & Scene Detection
imagehash
opencv-python
ffms2
VapourSynth
scenedetect[opencv]

# Voice Activity Detection
webrtcvad-wheels

# Optional AI audio features - install separately if needed:
# For AI features (audio-separator), install with:
#   pip install "audio-separator[gpu]"
